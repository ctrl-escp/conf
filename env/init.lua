-- Shared config (managed in repo — replace nvim-conf.lua to update)
require("nvim-conf")

-- ============================================================
-- Machine-local config
-- Add overrides, extra keymaps, env-specific settings here
-- ============================================================
